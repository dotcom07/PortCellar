[package]
name = "portcellar-cli"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[[bin]]
name = "portcellar"
path = "src/main.rs"

[dependencies]
portcellar-core = { path = "../portcellar-core" }
